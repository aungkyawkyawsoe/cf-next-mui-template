import CircleIcon from '@mui/icons-material/Circle';

const appConfig = {
    appName: 'LIZARD TMS',
    drawerWidth: 280,
    miniDrawerWidth: 65,
    listItemHeight: 48,
    defaultBaseRoute: '/dashboard',
    defaultNavIcon: CircleIcon,
}

export default appConfig;