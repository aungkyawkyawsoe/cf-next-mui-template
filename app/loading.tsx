import React from 'react'
import FallbackSpinner from '@components/Spinner'

const Loading = () => {
    return (
        <div>
            <FallbackSpinner />
        </div>
    )
}

export default Loading
